#pragma once

#include "types.h"

namespace Game {
    namespace Coop {
        class Setting {
            public:
                u64 startChangeWaterLevel(int);
                void reset();
                void setup(int);
                /* ... */
        };
    };
};
